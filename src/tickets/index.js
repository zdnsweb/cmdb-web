import TicketIcon from '@material-ui/icons/List';

import TicketList from './TicketList';
import TicketCreate from './TicketCreate';
import TicketEdit from './TicketEdit';

export default {
    list: TicketList,
    create: TicketCreate,
    edit: TicketEdit,
    icon: TicketIcon,
};
